apiVersion: apps/v1
kind: Deployment
metadata:
    annotations:
        container.apparmor.security.beta.kubernetes.io/nginx: unconfined
    labels:
        app: nginx
        nirmata.io/remediation-path: create-pr
        nirmata.io/resource-optimization: enabled
    name: nginx
spec:
    replicas: 1
    selector:
        matchLabels:
            app: nginx
    template:
        metadata:
            labels:
                app: nginx
        spec:
            containers:
                - image: nginx:latest
                  name: nginx
                  ports:
                    - containerPort: 80
                      hostPort: 80
                  resources:
                    limits:
                        cpu: 5000m
                        memory: 2800Mi
                    requests:
                        cpu: 50m
                        memory: 64Mi
                  securityContext:
                    capabilities:
                        add:
                            - SYS_ADMIN
                    privileged: true
            volumes:
                - hostPath:
                    path: /etc
                  name: host-vol
