apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  labels:
    app: nginx
  annotations:
    container.apparmor.security.beta.kubernetes.io/nginx: unconfined
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      hostUsers: false
      securityContext:
        seLinuxOptions:
          type: "container_t"
        seccompProfile:
          type: Unconfined
      volumes:
      - name: host-vol
        emptyDir: {}
      containers:
      - name: nginx
        image: nginx:1.19.0
        ports:
        - containerPort: 80
          hostPort: 80
        securityContext:
          privileged: true
          procMount: Unmasked
          capabilities:
            add:
            - SYS_ADMIN
          seLinuxOptions:
            user: "system_u"
            role: "system_r"
            type: "invalid_t"
          seccompProfile:
            type: Unconfined
